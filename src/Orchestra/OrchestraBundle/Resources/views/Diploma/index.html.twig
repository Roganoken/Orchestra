<h1>Diploma list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Fullname</th>
            <th>Certificationname</th>
            <th>Certificationdate</th>
            <th>Final</th>
            <th>Created</th>
            <th>Updated</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('diploma_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.fullName }}</td>
            <td>{{ entity.certificationName }}</td>
            <td>{% if entity.certificationDate %}{{ entity.certificationDate|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.final }}</td>
            <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.updated %}{{ entity.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('diploma_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('diploma_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('diploma_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
