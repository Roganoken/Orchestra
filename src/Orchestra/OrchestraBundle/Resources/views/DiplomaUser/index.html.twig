<h1>DiplomaUser list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Issuedate</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('diploma_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{% if entity.issueDate %}{{ entity.issueDate|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('diploma_user_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('diploma_user_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('diploma_user_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
