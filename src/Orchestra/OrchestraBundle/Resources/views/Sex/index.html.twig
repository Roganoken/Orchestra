<h1>Sex list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Gender</th>
            <th>Title</th>
            <th>Titleabbrev</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('sex_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.gender }}</td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.titleAbbrev }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('sex_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sex_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('sex_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
