{% if menus|length != 0 %}
<ul>
    {% for menu in menus %}
    <li>
        <a href="{{ path('livre_genre', { 'id': menu.id }) }}">{{ menu.libelle }}</a>
    </li>
   {% endfor %} 
</ul>
{% endif %}
