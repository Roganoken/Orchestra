<h1>SocialNetwork list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Url</th>
            <th>Baseurl</th>
            <th>Shareurl</th>
            <th>Logo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('social_network_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.url }}</td>
            <td>{{ entity.baseUrl }}</td>
            <td>{{ entity.shareUrl }}</td>
            <td>{{ entity.logo }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('social_network_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('social_network_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('social_network_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
