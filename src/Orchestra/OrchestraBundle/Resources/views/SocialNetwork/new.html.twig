<h1>SocialNetwork creation</h1>

<form action="{{ path('social_network_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('social_network') }}">
            Back to the list
        </a>
    </li>
</ul>
