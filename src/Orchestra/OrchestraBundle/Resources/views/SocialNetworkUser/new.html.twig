<h1>SocialNetworkUser creation</h1>

<form action="{{ path('social_network_user_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('social_network_user') }}">
            Back to the list
        </a>
    </li>
</ul>
