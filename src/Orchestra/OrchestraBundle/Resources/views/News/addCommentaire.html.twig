<h1>Ajouter un commentaire</h1>

<form action="{{ path('news_commentaire_create', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Ajouter</button>
    </p>
</form>