<h1>Galerie list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Parentid</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('galerie_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.parentId }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('galerie_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('galerie_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('galerie_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
