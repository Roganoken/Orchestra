<h1>Auteur creation</h1>

<form action="{{ path('auteur_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>
   

<ul class="record_actions">
    <li>
        <a href="{{ path('auteur') }}">
            Retour à la liste 
        </a>        
    </li>
</ul>